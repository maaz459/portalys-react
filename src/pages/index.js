import HomePage from "../modules/pages/Home/index";
import Layout from "../modules/layout/layout";
const Home = () => {
  return (
    <Layout>
      <HomePage />
    </Layout>
  );
};

export default Home;
